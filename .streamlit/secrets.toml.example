# Example secrets file - DO NOT COMMIT THIS WITH REAL VALUES
# Copy this file to .streamlit/secrets.toml and fill in your real values.
# The secrets.toml file is already in .gitignore

# App password (required)
password = "your_app_password"

# Supabase (optional – for persisting data on Streamlit Cloud)
# If set, all trip data (places, budget, notes, photos, etc.) is stored in the database
# so it survives app restarts. Leave commented for local file storage only.
# SUPABASE_URL = "https://xxxxxxxxxxxx.supabase.co"
# SUPABASE_KEY = "your_supabase_anon_key"

